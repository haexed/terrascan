# Terrascan - Environment Variables
# Configure API keys and database settings

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database

# NASA FIRMS API Configuration (REQUIRED)
# Get your key at: https://firms.modaps.eosdis.nasa.gov/api/
NASA_FIRMS_API_KEY=your_nasa_firms_api_key_here

# Air Quality API Configuration
# Primary: World AQI (RECOMMENDED - 10,000 requests/day free tier)
# Get your key at: https://aqicn.org/api/
WORLD_AQI_API_KEY=your_world_aqi_api_key_here

# Fallback: OpenAQ (OPTIONAL - used if WAQI fails)
# Get your key at: https://openaq.org/
OPENAQ_API_KEY=your_openaq_api_key_here

# NOAA Ocean Data - No API key required (completely free public API)
# Uses: https://api.tidesandcurrents.noaa.gov/

# OpenWeatherMap (OPTIONAL - configured in database, not env vars)
# API key stored in provider_config table via web interface
# Get your key at: https://openweathermap.org/api

# GBIF Biodiversity - No API key required (completely free)
# Uses: https://api.gbif.org/

# Task Control
TASK_ENABLED_FIRES=true
TASK_ENABLED_AIR_QUALITY=true
TASK_ENABLED_OCEAN=true
TASK_ENABLED_WEATHER=true
TASK_ENABLED_BIODIVERSITY=true

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=false
SECRET_KEY=your_secret_key_here
PORT=5000

# Railway Configuration (Production)
RAILWAY_TOKEN=your_railway_token_here

# Notes:
# Database Setup:
# - PostgreSQL: Set DATABASE_URL for production-like local development
# - Production uses Railway PostgreSQL (DATABASE_URL auto-configured)
#
# Data Sources (5 implemented):
# - NASA FIRMS: Fire detection data (NASA_FIRMS_API_KEY required)
# - World AQI: Primary air quality source (WORLD_AQI_API_KEY recommended, 10k/day free)
# - OpenAQ: Fallback air quality (OPENAQ_API_KEY optional)
# - NOAA Ocean: Ocean temperature/water levels (completely free, no key needed)
# - OpenWeatherMap: Weather data and alerts (configured in database, optional)
# - GBIF: Biodiversity data (completely free, no key needed)
#
# Required vs Optional Keys:
# REQUIRED for basic functionality:
#   - NASA_FIRMS_API_KEY (for fire data)
#   - WORLD_AQI_API_KEY or OPENAQ_API_KEY (for air quality data)
#
# OPTIONAL for enhanced features:
#   - OpenWeatherMap (via database config, adds weather layer)
#
# FREE (no keys needed):
#   - NOAA Ocean data
#   - GBIF Biodiversity data
#
# Security:
# - Keep this file LOCAL (already in .gitignore)
# - Never commit API keys to version control
# - For Railway deployment, set keys via: railway variables set KEY=value
