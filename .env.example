# TERRASCAN - Environment Variables
# Configure API keys and database settings

# Database Configuration
# For local development (SQLite - default)
# DATABASE_URL is automatically set to sqlite:///terrascan.db if not specified

# For production (PostgreSQL via Railway)
# DATABASE_URL=postgresql://username:password@host:port/database

# NASA FIRMS API Configuration
NASA_API_KEY=your_nasa_api_key_here

# OpenAQ API Configuration  
OPENAQ_API_KEY=your_openaq_api_key_here

# NOAA API Configuration
NOAA_API_KEY=your_noaa_api_key_here

# OpenWeatherMap API Configuration
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# GBIF Biodiversity - No API key required (completely free)

# Task Control (Admin Only)
# Control which tasks are enabled in production
TASK_ENABLED_FIRES=true
TASK_ENABLED_AIR_QUALITY=true
TASK_ENABLED_OCEAN=true
TASK_ENABLED_WEATHER=true
TASK_ENABLED_BIODIVERSITY=true

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=false
SECRET_KEY=your_secret_key_here
PORT=5000

# Railway Configuration (Production Only)
RAILWAY_TOKEN=your_railway_token_here

# Notes:
# Database Setup:
# - SQLite: Zero-config (default for local development)
# - PostgreSQL: Set DATABASE_URL for production-like local development
# - Production uses Railway PostgreSQL (DATABASE_URL auto-configured)
#
# Data Sources (5 implemented):
# - NASA FIRMS: Fire detection data (API key required)
# - OpenAQ: Air quality measurements (API key recommended)
# - NOAA Ocean: Ocean temperature/water levels (API key required)
# - OpenWeatherMap: Weather data and alerts (API key required)
# - GBIF: Biodiversity data (no API key required - completely free)
#
# Security:
# - Keep this file LOCAL only (already in .gitignore)
# - Never commit API keys to version control
