[deploy]
# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 30

# Sleep after 10 minutes of no traffic (saves $$$)
sleepApplication = true
