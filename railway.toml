[deploy]
# Restart policy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Health check
healthcheckPath = "/api/health"
healthcheckTimeout = 30

# Resource limits - minimal for low-traffic hobby project
numCpus = 0.5
memoryMB = 512
